/*package com.centerm.task;

import com.centerm.core.cache.builder.FunctionCacheBuilder;
import com.centerm.core.cache.builder.MenuCacheBuilder;
import com.centerm.service.cache.ICacheService;
import com.centerm.utils.SpringUtil;


*//**
 * 定时刷新缓存
 * @author CAIJIE
 *
 *//*
public class CacheTask {
	
	public void run() {
		ICacheService cacheService = (ICacheService) SpringUtil.getBean("cacheService");
		MenuCacheBuilder.init(cacheService);
		FunctionCacheBuilder.init(cacheService);
	}
}
*/